Your FIRST tool call MUST be Write of `/Users/alien/Projects/immigration-research/infra/immigration-fiscal/ledger_absolute_2026_09_17/RESULT_extension.md` with the line `**Verdict:** EXTENSION IN PROGRESS [UNVERIFIED]`; your second is to save this prompt verbatim as `BRIEF_extension.md` in the same directory. Repo `/Users/alien/Projects/immigration-research`. Raw data read-only; do not commit; do not edit other lanes except as stated.

Goal: close the two gaps left in the complete absolute account (`infra/immigration-fiscal/ledger_absolute_2026_09_17/`, read its README.md, RESULT.md, absolute_ledger.py, check_gates.py and params/PARAMS.md first). Both changes go into `absolute_ledger.py` and `check_gates.py`; add gates for each; the final run must keep every existing gate passing and reproduce the current step-13 endpoint (−253.931979bn) when the two new items are switched off.

**Part 1 — item D, district cost-to-serve differential.** The researcher lane staged the Census F-33 FY2024 district finance file and the NCES CCD LEA membership-by-race file under `~/research-data/immigration-fiscal/data/external/` (paths, sha256 and column notes in `params/params.json` → `staged_files` and `k12.f33_district_file_columns`; the CCD CSV is ~648 MB, read it in chunks or with a column subset). Build `district_differential.py` that: joins F-33 (NCESID) to CCD (LEAID); computes each district's per-pupil current spending (total current elementary-secondary spending ÷ fall membership, using the F-33 fields the researcher named); aggregates CCD membership to Hispanic, White non-Hispanic and total pupils per district; and for each state computes the enrollment-weighted per-pupil current spending faced by Hispanic pupils, by white pupils and by all pupils, and the differentials (Hispanic minus all, white minus all). Gate: the all-pupil weighted mean per state must land within 10% of the F-33 state summary per-pupil figure already in params (`k12.f33_per_pupil_current_spending_by_state`); report the ratio per state and drop districts with missing or implausible (<$3,000 or >$80,000) per-pupil values with a count. Write `derived/district_differential_by_state.csv` and add a `district` group to `params/params.json` (keys: `hispanic_minus_all_by_state`, `white_minus_all_by_state`, `all_pupil_mean_by_state`, `coverage_ratio_by_state`, each with status `verified`, source = the two staged files with their sha256, fetched = now, quote = the F-33 and CCD column names used). Then enable item D in `absolute_ledger.py`: Mexican-origin public pupils 5–17 (the ledger's pupil rule) are charged the Hispanic-minus-all differential of their state; the third-plus NH white reference is charged the white-minus-all differential; all-native reference zero. Keep the existing `dropped_no_verified_parameter` path for the case the group is absent. D goes on the marginality dial like K.

**Part 2 — item P, non-school state and local capital.** From the cached 2022 Census of Governments state-local table (`ledger_residual_agg_2026_09_16/_cache/22slsstab1.xlsx`, read how `residual_agg.py` and `absolute_ledger.py` already parse it) take total capital outlay by state minus elementary-and-secondary education capital outlay (item K already prices school capital from F-33; state the two sources' overlap in the RESULT), inflate with the existing `deflator.sl_deflator_2022_to_2024_ratio`, divide by the 2022 state populations the lane already uses for item G (same denominator convention), and charge per capita by state of residence. Insert P into the waterfall after K, on the marginality dial. National reconciliation: add P to the account lines and reduce the "state and local capital outlay" known-residual line accordingly. Gate: P summed over all civilian-household residents reconciles to the national non-school capital total within the population ratio.

**Run and report.** `OPENBLAS_NUM_THREADS=1 uv run --no-project python3 infra/immigration-fiscal/ledger_absolute_2026_09_17/absolute_ledger.py --params infra/immigration-fiscal/ledger_absolute_2026_09_17/params/params.json` then `... check_gates.py`; both from the repo root, both exit 0; paste tails into `RESULT_extension.md`. Report: D by group ($bn, per pupil, common-age gap contribution) and its sign; the per-state differentials for CA, TX, IL, AZ; P for the union and the white reference; the new union endpoint, complete common-age gap vs white, arms-matrix range and m*; and the off-switch reproduction of −253.931979bn. Update README.md and RESULT.md of the lane to carry the new items (append sections; do not delete the prior result). Return the RESULT_extension.md path and ≤10 lines.

MODEL SELF-REPORT (auto-added): the first line of your FIRST output (file or report message) MUST be your exact model ID copied verbatim from your own environment-info block. Model pins on this dispatch surface are known-unreliable; the parent reads this line back before trusting tier-sensitive work.
