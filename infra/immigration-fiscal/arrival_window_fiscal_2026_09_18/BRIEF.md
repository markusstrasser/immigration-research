Your FIRST tool call MUST be Write of `/Users/alien/Projects/immigration-research/infra/immigration-fiscal/arrival_window_fiscal_2026_09_18/RESULT.md` with `**Verdict:** BUILD IN PROGRESS [UNVERIFIED]`; second, save this prompt verbatim as `BRIEF.md` in that directory. Repo `/Users/alien/Projects/immigration-research`. Raw data read-only; do not commit; do not edit other lanes.

Goal: the all-age fiscal account for the Mexico-born split by arrival window, so the operator can see whether the people who came in roughly the last eight years are net positive, break-even or negative, and how the balance moves across cohorts. Read first `infra/immigration-fiscal/pronatal_equivalence_2026_09_18/lifetime_profiles.py` (the pattern: import the all-age lane's `analyze.py`, rebuild `state`, call `A.sufficient` and `A.account` for the `all_age_shared` scenario with full and replicate weights) and `infra/immigration-fiscal/all_age_ledger_2026_09_17/README.md` and `analyze.py` (group definitions, civilian domain, eight age bands, `standardized_gap`, `contrast`, replicate SE = sqrt(4/160 Σ(rep−full)²)).

Build `arrival_window_ledger.py` that: (1) reads the CPS year-of-entry field `PEINUSYR` and its code ranges from the held documentation (`cpsmar25.pdf` under sources or the CPS data dictionary already parsed by `gen_ledger_extension_2026_09_16/extend_ledger.py`; quote the code list in the RESULT); (2) splits the `mexico_born` group into windows: before 1990, 1990–99, 2000–09, 2010–15, 2016–2024/25 (the most recent window the codes allow; state its exact span), and reports the weighted population and mean age of each; (3) for each window computes, with full and 160 replicate weights: absolute partial balance ($bn and per person), the common-age gap per standardized person vs third-plus NH white and vs all natives (white-reference age shares, as the lane does), the age-matched gap ($bn), and the component split of the age-matched gap (tax, employer, sales, property, cash, noncash, school, lunch, medical) so the reader sees tax-side vs benefit-side by cohort; (4) per-band balance per person for each window (the eight bands), because recent arrivals are young and the absolute is age-structure-driven; (5) the same per-window figures under the complete-account central charges from `ledger_absolute_2026_09_17` for the flat per-capita items only (state-local general services by state, K-12 capital per pupil, excise credit, rest-of-federal per capita part) so a "complete" per-person figure exists per window; import that lane's per-capita vectors or recompute from its params, and say which; (6) the derivative view: a table of common-age gap by window (first difference between adjacent windows = first derivative; difference of differences = second derivative), with replicate SEs on each difference, and a plain statement of whether the trend is improving, flat, or worsening and whether it is accelerating; (7) if `sources/.../cps_asec_2026/asecpub26csv.zip` (income year 2025) exists and `ext.build` accepts it, repeat (3) on it and report the one-year change per window at fixed cohort as the duration derivative; if not, say so. Note explicitly that the US-born children of each window's arrivals sit in the second-generation group, not in the window, and report per window the number of own children under 18 in the SPM unit per adult so the reader can see the omitted dependents.

Gates: window populations sum to the lane's Mexico-born total 12.22m to within one person; the union of windows reproduces the stored `mexico_born` absolute (+6.63bn) and common-age gap (−6,562) to $0.01; SEs finite. Outputs `derived/*.csv`, `audit.json`, `RESULT.md` opening `**Verdict:**` with the tables and the exact commands. Run from the repo root with `OPENBLAS_NUM_THREADS=1 uv run --no-project python3 ...`; paste the tail. Return the RESULT path and ≤10 lines: per-window common-age gap and absolute per person, and the derivative statement.

AUDIT BOUND (required): If you verify filesystem paths, your response MUST start with these three lines (plain text, no markdown bold):
SEARCH_ROOT: <absolute path you searched>
GLOB_SCOPE: repo | monorepo | ~/Projects
VERDICT_REQUIRES: exists | content-match | not-found

MODEL SELF-REPORT (auto-added): the first line of your FIRST output (file or report message) MUST be your exact model ID copied verbatim from your own environment-info block. Model pins on this dispatch surface are known-unreliable; the parent reads this line back before trusting tier-sensitive work.
