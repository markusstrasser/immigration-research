# BRIEF (verbatim dispatch prompt, 2026-09-18)

Your FIRST tool call MUST be Write of `/Users/alien/Projects/immigration-research/research/immigration-employment-entry-displacement-2026-09-18.md` with `**Verdict:** PROBE IN PROGRESS [UNVERIFIED]` and a one-line purpose; your second is to save this prompt verbatim as `infra/immigration-fiscal/employment_entry_2026_09_18/BRIEF.md`. Repo `/Users/alien/Projects/immigration-research`; read `CLAUDE.md` (source tags, steel-man, disconfirmation mandatory) and `research/immigration-canon-citation-audit-2026-09-17.md` §on Jaeger–Ruist–Stuhler (JRS 2018, NBER w24285) and Dustmann–Schönberg–Stuhler 2017, which say the durable cost of low-skill immigration falls on native employment at labor-market entry, and that the past-settlement shift-share instrument is biased toward zero for short-run effects unless the lagged shock is controlled. No US replication of that estimand exists in the repo; that is the task.

Data: the local IPUMS microdata panel (read `~/.claude/projects/-Users-alien-Projects-immigration-research/memory/immigration-ipums-microdata-panel.md` for its location under `~/research-data/immigration-fiscal/derived/`, schema, and the foreign-born rule `BPL>=150`; Mexico-born is `BPL==200`; it holds ACS 1-year 2005–2023 and Census 2000/1990 samples with `MET2013` or `PUMA`/`CZ` geography and person weights). Do not modify it. Analysis directory `infra/immigration-fiscal/employment_entry_2026_09_18/` with DuckDB or pandas scripts; scripts over 10 lines in .py files; `uv run --no-project python3`.

Design, run exactly this and report every row: unit = metro (MET2013 or commuting zone, whichever the panel supports with stable boundaries) × year, 2005–2023. Outcome = employment-to-population ratio and labor-force participation of native-born (`BPL<150`), no college (`EDUC` below bachelor's, state the code), ages 18–29, separately for men and women, and a pooled 16–24 arm. Treatment = change in the Mexico-born share of the metro's population aged 18–64 (and an all-foreign-born arm). Estimators: (a) OLS in long differences (5-year and 10-year); (b) IV with the past-settlement instrument (metro's 1990 or 2000 Mexico-born share × national Mexico-born net inflow over the window); (c) the JRS specification: same IV plus the lagged instrument (previous window's predicted inflow) as a second instrument or control, which is their multiple-instrumentation fix; (d) a placebo: the instrument's effect on the outcome in the window before the shock. Cluster standard errors by metro; weight by native population. Report coefficients as percentage-point change in the native employment rate per 1-point rise in the Mexico-born population share, with 95% intervals, for every arm and window, in `derived/estimates.csv`. Gate: the instrument's first stage F must be reported for every IV row; report the JRS diagnostic (correlation of current and lagged predicted inflows across metros). Compare the magnitudes with Card 2001's 1–3 points for gateway cities and with DSS 2017.

Memo at the path above: verdict, design, the estimates table, the JRS check (does adding the lagged shock move the short-run coefficient, and in which direction), what the result does and does not identify (no legal status, ACS not CPS, metro not person, no wages), the disconfirmation section, and Sources. Neutral language. Return the memo path and ≤10 lines including the headline coefficient for the 5-year IV with and without the JRS correction.

OUTPUT DISCIPLINE (auto-added): Write your results to a file and return the file path — if no path is given, choose a descriptive one under .claude/reviews/. Your FIRST tool call MUST be Write a 'PROBE IN PROGRESS' stub at that path (if the path is under a research/ tree, include an [UNVERIFIED] provenance tag in the stub BODY TEXT, never only YAML frontmatter/HTML comments — the stop-research-gate strips those and blocks untagged research files), then append findings incrementally as you confirm them (do NOT batch and write at the end — a mid-run cutoff then loses everything).

MODEL SELF-REPORT (auto-added): the first line of your FIRST output (file or report message) MUST be your exact model ID copied verbatim from your own environment-info block. Model pins on this dispatch surface are known-unreliable; the parent reads this line back before trusting tier-sensitive work.
